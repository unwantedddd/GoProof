# GoProof

## Назва
**GoProof – Веб-платформа для проходження челенджів**

---

## Мета
Розробити сучасний веб-застосунок з **зручним інтерфейсом, гнучкою архітектурою та високою продуктивністю**, використовуючи **Node.js + Express.js** для бекенду та **React + TailwindCSS** для фронтенду.

---

## Основні сутності  
- **User** — користувач (id, name, email, password, role).
- **Challenge** — челлендж/завдання (id, title, description, start_date, end_date, status).
- **Participation** — участь у челленджі (id, user_id, challenge_id, join_date, status).
- **Proof** — доказ виконання (id, participation_id, file_url, description, submission_date, verification_status, admin_id).

---

## Можливості застосунку
- Адаптивний інтерфейс (**React + TailwindCSS**)  
- Перегляд і фільтрація челенджів      

---

## Використані технології

### Backend
- **Node.js** – середовище виконання  
- **Express.js** – веб-фреймворк  
- **PostreSQL** – база даних  
- **dotenv** – керування середовищем    

### Frontend
- **React.js** – побудова інтерфейсу  
- **Vite** – швидка збірка  
- **TailwindCSS** – стильова бібліотека  

---

## Структура проєкту

```
GoProof/
├── backend/              # Серверна частина
│   ├── dbqueries/        # SQL-скрипти
│   │   ├── create.sql
│   │   └── delete.sql
│   ├── src/
│   │   ├── db/
│   │   │   └── db.js     # Налаштування бази
│   │   └── server.js     # Точка входу сервера
│   └── .env              # Змінні середовища
│
├── frontend/             # Клієнтська частина
│   ├── public/           # Публічні файли (іконки, зображення)
│   ├── src/
│   │   ├── assets/       # Статичні ресурси
│   │   ├── components/   # Компоненти (Header, Footer, Layout)
│   │   ├── pages/        # Сторінки (Home, User і т.д.)
│   │   ├── App.jsx       # Головний компонент
│   │   └── main.jsx      # Точка входу
│   ├── index.html
│   └── vite.config.js
│
├── package.json          # Налаштування залежностей
└── README.md
```

---

## Як запустити проєкт

### 🔹 Необхідне ПЗ
1. **Node.js** (v18+) – [Завантажити](https://nodejs.org/)  
2. **PostgreSQL** - [Завантажити](https://www.postgresql.org)    

---

### Встановлення та запуск

1. Клонуйте репозиторій:
   ```bash
   git clone https://github.com/unwantedddd/GoProof.git
   cd GoProof
   ```

2. Встановіть залежності для **backend**:
   ```bash
   cd backend
   npm install
   ```

3. Створіть `.env` файл (приклад):
   ```
   POSTGRES_HOST=name
   POSTGRES_PORT=port
   POSTGRES_USER=username
   POSTGRES_PASSWORD=password
   POSTGRES_DB=GoProof
   ```

4. Запустіть сервер:
   ```bash
   npm run dev
   ```

5. Встановіть залежності для **frontend**:
   ```bash
   cd ../frontend
   npm install
   ```

6. Запустіть фронтенд:
   ```bash
   npm run dev
   ```

7. Відкрийте у браузері:
   ```
   http://localhost:5173
   ```

---

## Все готово!

Тепер ти можеш користуватись проєктом! Якщо щось не працює — перевір `.env` та налаштування БД 
